// Program to shuffle randomly shuffle elements
// Algo:
//   for i from n-1 downto 1 do
//	j = random integer such that 0 <= j <=i
//	exchange a[i] and a[j]

#include <stdio.h>

main()
{
	// seed
	//srand(time(NULL));
}
